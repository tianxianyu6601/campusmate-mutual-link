# CampusMate 重构进度日志

## 2026-08-08：产品行动计划

### 状态

- **规划状态：** complete
- **代码实施状态：** not_started
- **当前等待：** 用户确认计划和第一个实施阶段

### 已完成

- 确认登录后包括个人资料、自由组局、周期搭子匹配三个核心板块。
- 根据群像／群青组局截图拆解活动广场和两步发布流程。
- 根据 TP Date 梳理保存问卷、轮次报名、固定截止和统一公布结果的机制。
- 将项目拆分为基线、架构、数据、个人资料、自由组局、周期匹配、消息、安全测试和部署阶段。
- 定义 MVP 顺序、关键验收标准、风险边界和待确认问题。

### 本次创建的文件

- `task_plan.md`
- `findings.md`
- `progress.md`

### 本次未修改

- 未修改任何现有 Python 代码。
- 未修改数据库、配置、依赖或线上部署。
- 未运行迁移、邮件或匹配任务。

### 下一步

1. 用户确认行动计划。
2. 用户决定是否先执行“阶段 0：冻结基线与代码审计”。
3. 开始代码实施前确认周期时间、用户范围、地点方式、图片要求、联系方式和消息中心范围。

## 2026-08-08：阶段 0——冻结基线与代码审计

### 状态

- **阶段状态：** complete
- **开始原因：** 用户已明确批准执行阶段 0

### 已完成

- 确认 `main`、本地 `HEAD` 和 `origin/main` 均以 `972feb2` 为回滚基线。
- 只读核对线上登录页可达，浏览器控制台未发现错误。
- 盘点登录、页面路由、数据库、邮件、问卷、匹配算法、AI 和评估模块。
- 确认 SQLite 完整性正常，记录表结构、数据量和遗留 `login_sessions` 风险。
- 生成数据库备份并完成 SHA-256 一致性和只读完整性校验。
- 标记可复用资产、必须升级部分、Streamlit Cloud 持久化限制和迁移策略。
- 生成 `docs/BASELINE_AUDIT_2026-08-08.md`。

### 本阶段未修改

- 未修改任何 Python 业务代码。
- 未修改数据库内容、依赖或 Streamlit 配置。
- 未发送邮件、运行匹配任务或推送线上部署。

### 本阶段允许的操作

- 只读审计代码、配置、数据库结构、测试与 Git 基线。
- 创建数据库备份和基线审计报告。
- 更新规划文件。
- 不实施个人资料、自由组局或周期匹配的新功能。

## 测试结果

| 测试 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 规划文件范围检查 | 只新增三个规划文件 | Git 仅显示 `task_plan.md`、`findings.md`、`progress.md` 三个新增文件 | passed |
| Git 基线 | 本地与远端主分支一致 | `HEAD` 与 `origin/main` 均为 `972feb2` | passed |
| Python 自动化测试 | 现有功能无回归 | 63 项 `unittest` 测试全部通过，最终复跑约 3.9 秒 | passed |
| Python 编译检查 | 所有模块可编译 | `python -m compileall -q` 通过 | passed |
| 数据库源库检查 | SQLite 文件完整 | `PRAGMA integrity_check=ok` | passed |
| 数据库备份检查 | 哈希一致且可只读打开 | SHA-256 一致，`integrity_check=ok` | passed |
| 线上健康检查 | 登录页可访问且无控制台错误 | CampusMate 登录页可访问，错误数 0 | passed |

## 错误日志

| 时间 | 错误 | 解决方案 |
|---|---|---|
| 2026-08-08 | 首次从仓库上一级目录读取规划文件失败 | 改为从 `CampusMate_WebDeploy` 根目录读取；没有产生改动 |
| 2026-08-08 | 首次备份校验的混合引号转义导致 Python `SyntaxError` | 使用 `pathlib.Path.as_uri()` 以只读 URI 重跑，校验通过 |
| 2026-08-08 | 最终复跑误用了未安装的 `pytest`，随后一次解释器定位命令超时 | 确认项目测试基于标准库 `unittest`，用正确命令重跑 63 项并全部通过 |

## 五问重启检查

| 问题 | 答案 |
|---|---|
| 我在哪里？ | 阶段 0 已完成，代码与数据基线已冻结 |
| 我要去哪里？ | 等待批准阶段 1，再调整登录后信息架构 |
| 目标是什么？ | 建成登录后使用的完整校园社交与匹配平台 |
| 我学到了什么？ | 当前最大前置风险是会话持久化、数据库持久化、迁移和邮件可靠性，见 `findings.md` |
| 我做了什么？ | 完成 Git、线上、代码、数据库、测试和部署基线审计，见审计报告 |

## 2026-08-08：阶段 1——登录后信息架构

### 状态

- **阶段状态：** in_progress（本地完成，等待部署决定）
- **开始原因：** 用户已明确批准执行阶段 1

### 本阶段范围

- 登录前仅展示登录、注册和重置密码。
- 登录后建立首页、个人资料、组局广场、周期匹配、我的消息导航。
- 首页展示个人资料、自由组局、周期搭子匹配三个主入口。
- 增加统一的页面空状态，并保留现有问卷与匹配能力作为周期匹配下的旧功能。
- 补齐单击登录、刷新保持当前页面、退出立即失效和直接 URL 权限验证。
- 不实现阶段 2 之后的数据模型、个人资料表单、活动业务或周期调度。

### 已完成

- 建立登录后首页、个人资料、组局广场、周期匹配、我的消息五项主导航。
- 首页展示个人资料、自由组局、周期搭子匹配三个入口。
- 将现有行动卡、即时匹配、结果和 AI 洞察归入周期匹配流程，未删除旧功能。
- 新增统一页面介绍和空状态组件，不伪造活动、资料或消息数据。
- 新增 7 天服务端登录会话；浏览器保存随机令牌，数据库只保存 SHA-256 哈希。
- 登录令牌不进入 URL；刷新保持当前路由；退出先撤销服务端会话再清 Cookie。
- 生成 `docs/INFORMATION_ARCHITECTURE.md` 页面地图与权限表。

### 测试结果

| 测试 | 结果 | 状态 |
|---|---|---|
| Python 编译检查 | 全部模块通过 | passed |
| 自动化回归 | 67 项 `unittest` 全部通过 | passed |
| 单击登录 | 一次点击进入登录后首页 | passed |
| 刷新保持页面 | 刷新前后均为 `/activities`，且保持登录 | passed |
| 退出立即生效 | 一次点击回到登录页，退出按钮消失 | passed |
| 直接 URL 权限 | 未登录访问 `/cycle_match` 返回登录页，受保护标题不可见 | passed |
| 登录后首页视觉 | 三张主入口卡片和按钮文字均正常可见 | passed |
| 浏览器控制台 | 完整干净回归错误数为 0 | passed |

### 部署前阻塞项

- 当前线上账号仍存放在 Streamlit Community Cloud 本地 SQLite 中。
- 推送代码会触发应用更新，而官方不保证本地数据库在更新后保留。
- 在用户确认是否接受账号可能丢失的风险前，不推送阶段 1 到线上。

### 阶段 1 错误与修复

| 错误 | 修复 |
|---|---|
| 表结构只读命令引号转义失败 | 改用参数化查询，未修改数据库 |
| 8501 残留连接显示旧错误 | 使用干净 8502 实例验证 |
| 退出 Cookie 写入／删除竞争 | 顶部优先处理退出请求，停止每次 rerun 重写 Cookie |
| 回调中 `st.switch_page` 无效 | 改为按钮返回值分支切页 |
| Cookie 脚本全局变量重复声明 | 使用立即执行函数隔离作用域 |

## 2026-08-08：阶段 2——统一数据模型与服务层

### 状态

- **阶段状态：** in_progress（local_complete，hosted_postgresql_pending）
- **开始原因：** 用户已明确批准执行阶段 2

### 本阶段范围

- 本地 SQLite 与线上 PostgreSQL 共用数据库接口。
- 建立版本化、幂等、可测试的数据库迁移。
- 建立资料、兴趣、隐私、活动、申请、成员、匹配轮次、报名、快照、结果、邮件任务和审计表。
- 为重复申请、重复入组、超员批准和越权修改增加数据库与服务层保护。
- 不实现阶段 3 之后的业务页面。

### 本地实现结果

- 新增 `services/database.py`：本地 SQLite、线上 PostgreSQL 的统一连接、参数占位符、事务和完整性错误适配。
- 新增 `services/migrations.py`：两版幂等迁移；旧账号、验证码、旧问卷、旧通知和登录会话原地保留。
- 新增资料、兴趣、隐私、活动、申请、成员、匹配轮次、报名、资料快照、匹配结果、邮件任务和审计表。
- 新增 `services/platform_service.py`：资料保存、活动创建/申请/审批/取消、匹配轮次创建/报名接口。
- 活动审批在一个写事务内完成发起人校验、容量检查、成员写入、状态更新、邮件入队和审计写入。
- 新增 `docs/PLATFORM_DATA_DICTIONARY.md` 和 `docs/DATABASE_MIGRATION_GUIDE.md`。
- 新增 `scripts/migrate_database.py` 和默认只读预览的 `scripts/import_legacy_sqlite.py`。
- 本地 `campusmate_app.db` 已迁移到结构版本 2；原有 2 个账号保留。

### 阶段 2 测试结果

| 测试 | 结果 | 状态 |
|---|---|---|
| Python 编译检查 | 数据库、迁移、服务和维护脚本全部通过 | passed |
| 自动化回归 | 原有与新增共 77 项 `unittest` 全部通过 | passed |
| 幂等迁移 | 首次应用 1、2；重复执行不重复迁移 | passed |
| 旧账号保留 | 旧 `users` 记录迁移后保持不变 | passed |
| 事务回滚 | 中途异常后写入数量为 0 | passed |
| 重复申请 | 服务层拒绝，数据库唯一约束兜底 | passed |
| 重复入组 | 复合主键拒绝 | passed |
| 超员批准 | 第二个批准被拒绝，申请保持 pending | passed |
| 越权审批/取消 | 非发起人被拒绝，活动与申请状态不变 | passed |
| 周期报名重复 | 每轮每人只能报名一次，并保存资料快照 | passed |
| 旧数据导入预览 | `users=2`，未打印邮箱或数据库连接串 | passed |
| 登录浏览器回归 | 单击登录、刷新 `/activities` 保持、退出立即返回登录页 | passed |

### 当前外部阻塞

- 本地和服务层工作已完成，但当前没有配置托管 PostgreSQL `database_url`。
- 因此尚不能执行真实 PostgreSQL 迁移、导入 2 个旧账号，也不能把这一批改动安全部署到 Streamlit Cloud。
- 等用户提供或配置连接串后，先迁移和导入，再做线上登录回归；在此之前不推送部署。

### 阶段 2 错误与修复

| 错误 | 修复 |
|---|---|
| 首次误用未安装的 `pytest` | 沿用项目标准库 `unittest`，不增加测试依赖 |
| 新测试临时 SQLite 文件在 Windows 清理时报占用 | 显式关闭原生 SQLite 测试连接 |
| 旧本地 Streamlit 进程缓存了改造前的认证模块 | 核对 8501 端口 PID 后重启当前代码实例，再完成浏览器回归 |

## 2026-08-08：阶段 3——个人资料板块

### 状态

- **阶段状态：** in_progress
- **开始原因：** 用户已明确批准执行阶段 3

### 本阶段范围

- 将现有个人资料占位页替换为可编辑、可保存、刷新可恢复的正式页面。
- 实现头像、基础身份、分类兴趣、性格社交、时间地点、两段自我描述和联系方式。
- 实现字段级隐私设置、资料摘要卡、服务端完整度计算和周期匹配前必填校验。
- 为“拼一切”兴趣分类和头像持久化增加向后兼容的数据库迁移。
- 不开发活动浏览/发布、活动申请审批、轮次调度或匹配结果业务页面。

### 本地实现结果

- 将 `pages/profile.py` 从占位页替换为四标签资料编辑器，覆盖基础资料、头像、兴趣与性格、时间与期待、字段级隐私设置。
- 新增资料摘要卡和服务端资料完整度计算；客户端不能伪造完整度。
- 新增周期匹配报名必填校验：昵称、学校、兴趣、时间、地点和两段描述缺一不可。
- 新增迁移版本 3：头像 Data URL 持久化，并安全扩展 `share`（拼一切）兴趣分类，已有兴趣数据原样迁移。
- 头像仅接受 PNG/JPEG/WebP，服务层校验 Base64、文件签名和 750 KB 上限。
- 新增按真实共同活动/匹配关系过滤资料字段的读取接口，资料所有者始终可以查看自己的完整资料。
- 首页个人资料入口更新为“可编辑 · 已保存”；自由组局和后续阶段未实施。
- 本地测试账号已保存昵称、院系、兴趣、时间、地点及两段描述，刷新 `/profile` 后内容保持，完整度为 90%，显示“已满足周期匹配资料要求”。

### 阶段 3 测试结果

| 测试 | 结果 | 状态 |
|---|---|---|
| Python 编译检查 | 资料页、首页、迁移、服务和测试文件全部通过 | passed |
| 自动化回归 | 共 83 项 `unittest` 全部通过 | passed |
| 幂等迁移 | 本地结构版本为 3，重复执行 `applied=none` | passed |
| 服务端完整度 | 忽略客户端提交分数，按实际资料计算 | passed |
| 报名前必填校验 | 资料不完整时服务层拒绝周期报名 | passed |
| 隐私过滤 | 仅自己、已匹配、共同活动成员、所有已登录用户四级关系通过 | passed |
| 头像校验 | 合法图片签名通过，伪造 Data URL 被拒绝 | passed |
| 浏览器保存与刷新 | 保存后摘要、兴趣、时间地点和两段描述均保持 | passed |
| 首页状态 | 显示“可编辑 · 已保存”，并保留其他阶段原状态 | passed |

### 阶段 3 错误与修复

| 错误 | 修复 |
|---|---|
| 首轮新增隐私测试断言误插入相邻测试，导致未定义 `payload` | 将断言移回资料测试并重新跑完整 83 项回归 |
| 页面未展示的旧 `social/travel` 兴趣可能在保存时丢失 | 保存时原样合并这两类旧标签，避免隐式删除 |

### 当前外部阻塞

- 当前仍未配置托管 PostgreSQL `database_url`，因此阶段 3 只完成了本地 SQLite 与浏览器验收。
- 为避免把线上已有 SQLite 账号与新结构割裂，在真实 PostgreSQL 迁移、旧账号导入和线上登录回归完成前不推送部署。

## 2026-08-08：阶段 4——自由组局浏览与发布

### 状态

- **阶段状态：** in_progress
- **开始原因：** 用户已明确批准执行阶段 4

### 本阶段范围

- 将组局广场占位页替换为真实活动列表、关键词/分类/时间/地点/状态筛选和活动详情。
- 实现两步发布：先选择分类，再填写标题、描述、图片、时间、地点、人数、可见范围和审核设置。
- 实现草稿、发布、满员、结束和取消状态在发布者侧的展示与阶段 4 允许的状态操作。
- 提供“我发布的/我参加的”活动视图，但不实现申请、审批、退出、移除成员或邮件。
- 未登录用户继续无法直接访问组局页面。
- 当前仍不部署线上，待托管 PostgreSQL 接通后再迁移和验收。

### 服务层进度

- `create_activity` 现支持草稿或直接发布，并完整校验分类、自定义分类、描述、时间、地点、人数、可见范围及图片文件签名。
- 新增活动列表、详情、服务端筛选、邀请可见性、我的活动、编辑、发布草稿和主动结束接口。
- 活动编辑使用版本号防止旧页面覆盖新数据；发布者权限、成员数下限和状态转换均在事务内校验。
- 阶段 4 新增 4 组服务测试；当前 `tests.test_platform_service` 共 15 项全部通过。

### 阶段 4 错误与修复

| 错误 | 修复 |
|---|---|
| 连续查询本地 Streamlit 文档时，`st.text_input` 的图标字符触发 Windows GBK 输出错误 | 改用 Python `inspect.signature` 读取精确 API 签名 |
| 活动地点首版传入 `st.selectbox(default=...)`，但当前 1.61 只支持 `index` | 将历史自定义地点合并进 options，再用稳定 index 预选 |
| 首次浏览器进入活动页时运行进程报旧模块缺少 `end_activity`；独立解释器确认当前源码已正常导出全部新接口 | 重启本地 Streamlit 实例，清除进程级 Python 模块缓存后继续验收 |
| 浏览器自动化首次按不含 Material 图标的精确按钮名查找“发布活动”失败 | 使用页面真实可访问名称 `add_circle 发布活动` 后继续；产品按钮本身可正常点击 |

### 浏览器验收进度

- 测试账号从首页一次点击进入组局广场，关键词、分类、时间、地点、排序和状态控件全部正常渲染。
- 两步发布流程已实测：选择“运动”，填写标题、描述、未来时间和邱德拔体育馆，成功创建本地活动。
- 发布后自动进入带 `?activity=...` 的详情 URL，详情显示分类、发起人、时间、地点、人数、介绍、可见范围和发起人操作。
- 浏览器刷新后仍停留在相同活动详情，登录状态和详情数据均保持。
- 浏览器实测草稿不出现在“发现活动”，但会出现在“我的活动”；草稿可继续编辑并发布。
- 实测发布者结束活动后状态立即变为“已结束”；取消活动会先显示确认对话框，确认后“已取消”立即生效并从发现列表隐藏。
- 1280 像素宽截图发现标题右侧操作列接近视口边缘，已将“发布活动”改到标题下方，保证窄屏也始终可见。

### 阶段 4 最终结果

- **本地阶段状态：** complete
- 组局广场支持发现活动/我的活动、关键词、分类、时间、地点、状态筛选，以及最近开始/最新发布/人气排序。
- 活动详情由 URL `activity` 参数定位，浏览器刷新后仍保留同一活动详情。
- 两步发布支持学习交流、运动、社交、娱乐、出行、拼一切和自定义分类，并可保存草稿或直接发布。
- 活动表单覆盖标题、3000 字描述、1 MB 图片、开始/结束时间、地点、2–100 人、可见范围和审核设置。
- 发起人可编辑未来活动、发布草稿、主动结束或经确认后取消；取消活动不会出现在发现列表。
- “我的活动”同时包含自己发起和正式参加的活动；草稿和取消活动仅对发起人展示。
- 首页自由组局状态已更新为“浏览与发布可用 · 申请待开放”。
- 本地浏览器创建了 2 条验收活动：一条已结束、一条已取消；没有在线数据变更或邮件发送。

### 阶段 4 最终测试

| 测试 | 结果 | 状态 |
|---|---|---|
| Python 编译检查 | 活动页、首页、服务与测试模块全部通过 | passed |
| 自动化回归 | 87 项 `unittest` 全部通过 | passed |
| 数据库迁移 | `schema_version=3`，重复运行 `applied=none` | passed |
| 草稿可见性 | 发现列表不可见，我的活动可见 | passed |
| 发布与立即可见 | 发布后广场立即出现活动卡 | passed |
| 详情刷新 | URL 和详情内容刷新后保持 | passed |
| 搜索与分类 | “羽毛球”关键词和运动分类均能命中 | passed |
| 草稿编辑发布 | 预填数据正确，编辑后转为报名中 | passed |
| 结束与取消 | 状态立即更新；取消前显示确认对话框 | passed |
| 首页状态 | 阶段 4 状态和说明文字已同步 | passed |

### 当前外部阻塞

- 线上仍未配置托管 PostgreSQL `database_url`，因此阶段 4 没有推送到 Streamlit Cloud。
- 部署前仍需先迁移结构、导入线上旧账号，并使用至少两个账号完成“发布者发布、另一用户立即看见”的线上验收。

## 2026-08-09：阶段 5——自由组局申请、审批与邮件

### 状态

- **阶段状态：** in_progress（local_complete，hosted_postgresql_pending）
- **开始原因：** 用户已明确批准执行阶段 5，并授权使用测试账号完成本地浏览器验收

### 本阶段范围

- 参与者提交申请理由，发起人查看待处理申请并同意或拒绝。
- 同意时在事务内重新检查状态和人数，避免重复成员和并发超员。
- 支持申请者撤回、成员主动退出、发起人移除成员以及取消活动。
- 活动申请、审批结果和活动取消写入站内提醒与持久邮件任务；邮件发送失败不回滚业务数据。
- 满员后停止新申请和继续批准；退出或移除后恢复为可申请状态。
- 不实现周期匹配轮次、算法、消息中心聚合或其他后续阶段。

### 阶段 5 错误与修复

| 错误 | 修复 |
|---|---|
| 首次代码盘点误查不存在的 `services/email_service.py` | 根据检索结果改读 `services/auth.py` 中现有 SendGrid/Resend/SMTP 适配器 |

- 阶段 5 实施记录：一次同时修改消息页和首页的补丁因首页原文不匹配被整体拒绝；已拆分为精确补丁，未造成半写入。
- 阶段 5 浏览器验收：重启了缓存旧模块的本地 Streamlit 进程；消息空状态首次暴露共享组件的无效箭头图标，已改为原生 Material 图标。
- 阶段 5 交付检查：邮件任务脚本直接运行时缺少项目根目录导入路径；已补齐入口路径，避免部署任务以文件方式启动时报错。

### 阶段 5 最终结果

- 申请者可填写 500 字以内理由；待审申请可撤回并重新提交，拒绝结果不会被覆盖。
- 发起人可查看待审申请并同意或拒绝；成员名单仅活动成员可见，发起人可移除非发起人成员。
- 免审核活动可直接加入；审核和直接加入均在事务内重查容量，满员后停止新申请和批准。
- 成员退出或被移除后释放名额并恢复报名中；取消活动会处理待审申请并通知所有受影响成员。
- 活动事件同时写入独立站内通知表和持久邮件任务；邮件失败按指数间隔重试，不回滚业务状态。
- 消息中心支持未读筛选、单条已读、全部已读和跳回活动详情；未实现阶段 8 的跨业务聚合。
- 本地测试账号发布并取消了 1 条阶段 5 验收活动；没有修改线上网站，也没有向其他账号发送邮件。

### 阶段 5 最终测试

| 测试 | 结果 | 状态 |
|---|---|---|
| 自动化回归 | 97 项 `unittest` 全部通过 | passed |
| 并发容量 | 两个申请并发争抢最后名额，仅一个批准成功 | passed |
| 邮件失败隔离 | 申请保持 `pending`，邮件任务进入可重试 `failed` | passed |
| 数据库迁移 | 本地 `schema_version=4`，旧活动和账号保留 | passed |
| 浏览器发布 | 测试账号两步发布活动，详情立即显示申请管理与成员名单 | passed |
| 浏览器取消 | 确认后状态立即变为“已取消”，发现列表不再展示 | passed |
| 消息中心 | 完整网站外壳中正常渲染，无 Streamlit 异常 | passed |
| 邮件任务脚本 | 直接运行返回任务计数 JSON | passed |

### 当前外部阻塞

- 线上仍未配置托管 PostgreSQL `database_url`；为了避免 Streamlit Cloud 重启后丢失活动、申请和通知，阶段 5 未推送或部署。
- 线上闭环验收需要第二个测试账号，或在部署后由另一位组员申请测试账号发布的活动。

## 2026-08-09：阶段 1–5 线上同步

- 用户已配置托管 PostgreSQL，并确认只迁移 2 个账号、1 份规范化个人资料及兴趣/隐私设置，不迁移 3 条本地测试活动。
- 本地 Secrets 文件存在、包含 `database_url` 且被 `.gitignore` 排除；连接串未输出。
- 首次只读连接发现本地缺少 `psycopg[binary]`，已按 `requirements.txt` 安装；沙箱外 `SELECT 1` 连接成功。
- 旧导入脚本仅覆盖 `user_profiles`，已扩展到规范化 `profiles`、`profile_interests` 和 `profile_privacy`，仍明确排除全部活动与通知表。
- 迁移后只读核对通过：账号密码哈希、规范化资料与源库完全一致；活动、申请、成员、通知、邮件任务和登录会话均为 0。
- 为避免本地 Secrets 存在时自动化测试误连生产 PostgreSQL，新增仅由测试命令显式启用的 `CAMPUSMATE_FORCE_SQLITE` 隔离开关；线上不设置该变量。
- 第一次暂存密钥扫描命令因 PowerShell 正则引号解析失败而未执行，未修改文件；已拆分为简单、可核对的独立扫描命令。

## 2026-08-09：GUI 与性能专项修复

### 已完成

- PostgreSQL 增加最多 4 个连接的进程内连接池，并加入迁移检查缓存。
- 过期登录会话清理改为每个数据库每 15 分钟最多一次。
- 已验证会话在同一 Streamlit 会话内走快速路径；未变化的页面状态不再每次 rerun 写库。
- 个人资料保存按钮改为适中横向尺寸，文字和图标保持单行。
- 主题主色由黑色改为蓝色，主按钮统一蓝底白字；多选标签统一浅蓝底深色字。
- 移除会覆盖按钮和标签内部文字的全局 `span`/`p` 黑色强制规则。

### 验证结果

| 检查 | 结果 | 状态 |
|---|---|---|
| 自动化回归 | 100 项 `unittest` 全部通过 | passed |
| PostgreSQL 冷连接登录 | 约 2.30 秒 | passed |
| PostgreSQL 热连接登录 | 约 0.35 秒 | passed |
| 资料/活动数据库读取 | 各约 0.45 秒 | passed |
| 本地真实单击登录 | 约 1.11 秒进入首页 | passed |
| 保存按钮 | 136×50，横向单行 | passed |
| 发布活动按钮 | 蓝底白字，文字可见 | passed |
| 多选地点标签 | 浅蓝底深色字，文字可见 | passed |
| 登录后五个主页面控件扫描 | 无低对比度按钮或多选标签 | passed |

## 2026-08-09：第二轮 GUI 与活动资料专项

- 用户截图确认“保存草稿”“发布活动”仍被全局表单按钮宽度规则挤成正方形并导致文字竖排。
- 用户要求发布者和申请者各自在个人资料中至少填写邮箱、QQ、微信之一。
- 用户要求活动成员名单可点击查看按现有隐私关系允许公开的个人资料。
- 用户要求发布活动有明确的自定义地点选项。
- 用户线上实测登录需要重复填写邮箱并点击两次，部分登录后导航也需要点击两次；当前开始审计 Cookie 重定向、会话恢复、按钮路由与重复 rerun。

### 本地完成与验证

- 修复 Cookie iframe 自跳转、活动查询参数重复 rerun、消息页查询参数被页面切换清空三个导航根因。
- 新增数据库迁移 v5：`profiles.contact_qq`；旧 SQLite 资料导入对缺失 QQ 列保持兼容。
- 发布者与申请者至少一种联系方式校验已下沉到事务服务层；草稿仍可先保存。
- 活动成员和待审核申请增加“查看资料”，严格复用字段级隐私过滤。
- 发布表单增加明确的自定义地点输入；活动提交按钮和个人资料保存按钮均实测为横向单行。
- 101 项 `unittest` 全部通过，`compileall` 通过，`git diff --check` 通过。
- 本地浏览器：登录约 0.6 秒、退出约 0.5 秒、刷新保留登录与当前页、个人资料/组局广场单击进入约 3.2 秒。
- 待办：提交、推送，等待 Streamlit Cloud 部署后执行线上回归。
- 首次线上回归：单击登录约 2.4 秒成功，但刷新后回到登录页；确认是 Cloud iframe 中登录瞬时 Cookie 组件被页面切换移除，已增加稳定的已登录外壳 Cookie 写入器，待二次部署验证。
- 第二次线上回归：稳定挂载 `st.html` 后仍无法跨 Cloud 外层 iframe 刷新恢复；已切换到 Extra Components 0.1.81 双向 CookieManager，待第三次部署验证。
